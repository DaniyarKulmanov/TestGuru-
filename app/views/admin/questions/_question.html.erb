<tbody>
  <tr>
    <td class="text-center"><%= question.body %></td>
    <td class="text-center"><%= link_to t('show'), admin_question_path(question) %></td>
    <td class="text-center"><%= link_to t('edit'), edit_admin_question_path(question) %></td>
    <td class="text-center"><%= link_to t('delete'), admin_question_path(question),
                     method: :delete,
                     data: { confirm: t('confirm', name: question.body) } %></td>
  </tr>
</tbody>