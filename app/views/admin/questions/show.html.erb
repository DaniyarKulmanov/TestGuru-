<h3><%= t('activerecord.attributes.question.body') %>: <%= @question.body %></h3>

<%= link_to t('new', name: t('activerecord.models.answer.one').downcase), new_admin_question_answer_path(@question) %>

<h2><%= t('activerecord.models.answer.many').capitalize %></h2>

<table class="table table-bordered border-info">
  <thead>
  <tr>
    <th class="text-center"><%= t('activerecord.attributes.answer.body') %></th>
    <th colspan="3" class="text-center"><%= t('actions') %></th>
  </tr>
  </thead>

    <%= render @question.answers %>

</table>

<p> <%= link_to t('edit'), edit_admin_question_path(@question) %></p>
<p> <%= link_to t('back'), admin_test_path(@question.test_id) %> </p>
