<%= form_with model: [@question, @answer] do |form| %>
  <%= render 'shared/errors', resource: @answer %>

  <div>
    <%= form.label :body %><br>
    <%= form.text_area :body %><br>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>