<tbody>
<tr>
  <td><%= answer.body %></td>
  <td><%= link_to 'show', answer_path(answer) %></td>
  <td><%= link_to 'edit', edit_answer_path(answer) %></td>
  <td><%= link_to 'delete', answer_path(answer),
                  method: :delete,
                  data: { confirm: 'Confirm deletion' } %></td>
</tr>
</tbody>