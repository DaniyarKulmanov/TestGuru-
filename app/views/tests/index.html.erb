<h2><%= t('.header') %></h2>

<table>
  <thead>
    <tr>
      <th><%= t('helpers.label.test.title') %></th>
      <th><%= t('helpers.label.test.level') %></th>
      <th><%= t('.question_count') %></th>
      <th><%= t('.actions') %></th>
    </tr>
  </thead>
  <%= render @tests %>

</table>