<tbody>
  <tr>
    <td><%= test.title %></td>
    <td class="text-center"><%= test.level %></td>
    <td class="text-center"><%= test.questions.count %></td>
    <td class="text-center"><%= button_to t('test.start'), begin_test_path(test) %></td>
  </tr>
</tbody>
