<%= render "hello" %>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= t('.unconfirmed_email', email: @resource.unconfirmed_email) %>.</p>
<% else %>
  <p><%= t('.email_changed', email: @resource.email) %>.</p>
<% end %>
